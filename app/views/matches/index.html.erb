<!-- Matches -->
<div class="matches">
	<div class="profile">
		<%= render 'partials/conversations' %>
		<%= link_to "Back to Fetching!", users_path, class: 'back-btn' %>
		
		<div class="">
			<div class="bio col-sm-4">
				<%= image_tag 'User Image' %>
				<h3 class="bio-name"><%= @friends.first.dog_name %> &amp <%= @friends.first.fname %></h3>
				<p class="bio-description"><%= @friends.first.bio %></p>
			</div>
			
			<%= render 'partials/messenger' %>
		</div>
	</div>
</div>